program fact;
var n: i32;

    procedure factorial(n: i32): i32
    begin
        if n = 0 then
            return 1;
        else
            return n * factorial(n - 1);
    end

begin
    read(n);
    write(factorial(n));
end